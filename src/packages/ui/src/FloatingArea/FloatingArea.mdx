---
menu: Utils
name: Floating Area
---

import { useState } from 'react';
import { Link, Playground, Props } from 'docz';
import { Button } from '../Button';
import { List, ListTitle, ListButton } from '../List';
import { FloatingArea } from './FloatingArea';

# Floating Area

Helper component used by `@rkta/ui` to bind stuff to a DOM Elements.

## Import

```javascript
import { FloatingArea } from '@rkta/ui';
```

## Examples

<Playground>
  {() => {
    const [target, setTarget] = useState(null);
    const bind = (event) => setTarget(event.target);
    return (
      <div style={{ display: 'flex', justifyContent: 'space-between', padding: 8 }}>
        <Button bgColor="primary" onClick={bind}>
          Target 1
        </Button>
        <Button bgColor="primary" onClick={bind}>
          Target 2
        </Button>
        <Button bgColor="primary" onClick={bind}>
          Target 3
        </Button>
        <FloatingArea align="bottom" producer={target} active={!!target} offset={8}>
          <List rize={1}>
            <ListTitle>Floating Area</ListTitle>
            <ListButton nowrap>Option 1</ListButton>
            <ListButton nowrap>Option 2</ListButton>
            <ListButton nowrap>Option 3</ListButton>
          </List>
        </FloatingArea>
      </div>
    );
  }}
</Playground>

## Props

<Props of={FloatingArea} />

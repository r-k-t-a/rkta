import { AddonProps } from '../../Addon/Addon.type';

export type Props = AddonProps & {
  center?: boolean;
  flexEnd?: boolean;
  spaceAround?: boolean;
  spaceBetween?: boolean;
  spaceEvenly?: boolean;
};

---
menu: UI
name: Provider
---

import { Props } from 'docz'
import Provider from './Provider'

# Provider
Provider is the essential part of `@rkta/ui` package.
It creates context for the rest components.

<Props of={Provider} />
